<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h1> I am an airline page </h1>
    <img src="<%= airline.img_url%>" alt="Airline image">
    <br>
    <%= airline.name%>
    <br>
    <%= airline.description%>

    <h1> Trips Planned </h1>

    <% for(let i=0; i<trips.length; i++){ %>
      <h3> <%= trips[i].name %> </h3>
      <h5> <%= trips[i].date %> </h5>
      <a href="/airline/<%= trips[i].airline_id %>"> <%= trips[i].airline_name %> </a>
      <h5> <%= trips[i].origin %> </h5>
      <h5> <%= trips[i].destination %> </h5>
      <a href="/trip/<%= trips[i].id %>">Edit Trip</a>
      <hr>
  <%  } %>
  </body>
</html>
